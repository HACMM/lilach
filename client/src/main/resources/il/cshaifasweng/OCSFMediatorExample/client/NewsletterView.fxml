<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx/23.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="il.cshaifasweng.OCSFMediatorExample.client.NewsletterController"
            prefHeight="600" prefWidth="800"
            style="-fx-background-image: url('/images/flower_background.png');
                   -fx-background-repeat: no-repeat;
                   -fx-background-size: cover;
                   -fx-background-position: center;
                   -fx-padding: 20;">

    <VBox alignment="TOP_CENTER" spacing="15"
          style="-fx-background-color: rgba(255,255,255,0.85);
                 -fx-background-radius: 12;
                 -fx-padding: 30;"
          AnchorPane.topAnchor="40"
          AnchorPane.bottomAnchor="40"
          AnchorPane.leftAnchor="100"
          AnchorPane.rightAnchor="100">

        <!-- 🔹 כפתור Back -->
        <Button text="← Back"
                onAction="#onBackClicked"
                style="-fx-background-color: transparent;
                       -fx-text-fill: #a64f73;
                       -fx-font-size: 15px;
                       -fx-font-weight: bold;
                       -fx-cursor: hand;
                       -fx-padding: 5 10 5 10;"
                alignment="CENTER_LEFT"
                maxWidth="Infinity"/>

        <!-- 🔹 כותרת -->
        <Label text="Send Newsletter to Subscribers"
               style="-fx-font-size: 22;
                      -fx-font-weight: bold;
                      -fx-text-fill: #2c3e50;"/>

        <Separator/>

        <!-- 🔹 שדה נושא -->
        <HBox alignment="CENTER_LEFT" spacing="10">
            <Label text="Subject:"
                   style="-fx-font-size: 14; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
            <TextField fx:id="subjectField"
                       prefWidth="400"
                       promptText="Enter email subject..."
                       style="-fx-background-radius: 8; -fx-border-radius: 8; -fx-border-color: #ddd; -fx-background-color: #fff;"/>
        </HBox>

        <!-- 🔹 שדה הודעה -->
        <Label text="Message:"
               style="-fx-font-size: 14; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
        <TextArea fx:id="messageArea"
                  prefRowCount="8" prefWidth="600"
                  promptText="Write your message here..."
                  wrapText="true"
                  style="-fx-background-radius: 8; -fx-border-radius: 8; -fx-border-color: #ddd; -fx-background-color: #fff;"/>

        <Separator/>

        <!-- 🔹 כפתור שליחה -->
        <HBox alignment="CENTER" spacing="16">
            <Button fx:id="sendBtn"
                    text="Send to All Subscribers"
                    onAction="#onSendClicked"
                    style="-fx-background-color: #e7b3d1;
                           -fx-text-fill: white;
                           -fx-font-weight: bold;
                           -fx-background-radius: 20;
                           -fx-padding: 8 25 8 25;
                           -fx-cursor: hand;"/>
        </HBox>

        <!-- 🔹 הודעת מערכת -->
        <Label fx:id="statusLabel"
               alignment="CENTER"
               maxWidth="600"
               style="-fx-text-fill: #b00020;
                      -fx-font-weight: bold;
                      -fx-font-size: 14;"
               visible="false"/>

    </VBox>
</AnchorPane>
